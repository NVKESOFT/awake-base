#ifndef DEFS_H
#define DEFS_H

enum resolution { SCREEN_WIDTH = 1280, SCREEN_HEIGHT = 960 };
enum delay { DELAY_ONE_FRAME = 16, DELAY_TWO_FRAMES = 32,
             DELAY_THREE_FRAMES = 48, DELAY_FOUR_FRAMES = 64 };
enum time_in_ms { MS_ONE_SECOND = 1000, MS_TWO_SECONDS = 2000 };

#endif
